<?xml version="1.0" encoding="utf-8"?>
<!-- API 31+: ni cloud backup ni transferencia dispositivo-a-dispositivo de ningún dato de la app.
     Complementa android:allowBackup="false" del manifest (ver el comentario ahí): la sal en
     SharedPreferences no sirve sin la clave del Keystore que la envuelve, y la DB cifrada con
     persona/nota es local-only (R-PV-02). Moverla a otro equipo es el restore E2E de ADR-003. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
